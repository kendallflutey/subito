  <div class="index-container">
  <div class= "full-show-container">

    <div class="categories-full-right-pane">

      <div class="choose-category">

        <div class="categories-right-pane-header">
            <h3 class="show-nearby">Shoing nearby deals in: </h3>
        </div>

        <div id="category-menu">
          <div id="select-wrapper">
              <select id="page-select" class="styled">
              <% @categories.each do |category| %>
               <option <% if category== @category%>selected = "selected" <% end %>value="<%= category_path(category) %>"><%= category.name %></option>
              <% end %>
              </select>
          </div>
        </div>

        <div class="stripe">
          <div class="line line-btm"></div>
        </div>
      </div>

      <div class="deal-display-container">
        <% @deals.each do |deal| %>
          <div class="deal-display-element" style="<%=deal_background_image(deal) %>">
            <div class="deal-title-box">
              <h2><%= deal.title %></h2>
            </div>
            <div class="deal-display-description">
              <h5><%= deal.description %></h5>
            </div>
            <div class="deal-display-footer">
              <h4> <%= deal.finish_time.strftime("%A %I:%M %P") %> </h4>
            </div>
          </div>
        <% end %>
      </div>
    </div>


    <div class="map-canvas-container">

        <ul class="map-view-navbar">

          <li><h2><%= @category.name %></h2></li>


          <li><img src="/assets/radius.png" id="radius"></li>
        </ul>

          <ul class="map-view-navbar">
          g  <li><h2><%= @category.name %></h2></li>
            <li><img src="/assets/radius.png" id="radius"></li>
          </ul>

          <div id="map-canvas"></div>
          <div id="nav_bottom"></div>
    </div>

  </div>
</div>

<script type="text/javascript">
var categoryId = <%= @category.id %>;
$(document).ready(function() {
  getCoords();

});
</script>

<script id="deal-template" type="text/x-handlebars-template">
  <div id="popup_deal">
    <div id="deal_image"></div>
    <h4>{{ title }}</h4>
    <p id="popup-description">{{ description }}</p>
    <p id="pop-up-timer"></p>
  </div>
</script>
